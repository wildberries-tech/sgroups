
export function RName() {
    return (
        <ul>
            <li class="text-justify">Длина значения поля не должна превышать 256 символов.</li>
            <li class="text-justify">Значение поля должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">Значение должно быть уникальным в рамках типа ресурса.</li>
        </ul>
    )
}

export function RCidr() {
    return (
        <ul>
            <li class="text-justify">Значение поля должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">В пределах одной Security Group и направления трафика (I/E), необходимо обеспечить, непересекаемость диапазонов адресов подсетей.</li>
            <li class="text-justify">Подсеть должна соответствовать формату записи, определенному в RFC 4632.</li>
        </ul>
    )    
}

export function RFqdn() {
    return (
        <ul>
            <li class="text-justify">Длина значения поля не должна превышать 256 символов.</li>
            <li class="text-justify">Значение поля должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">FQDN должна соответствовать формату записи, определенному в RFC 1034,1035,1123.</li>
        </ul>
    )    
}

export function RSgroupSet() {
    return (
        <ul>
            <li class="text-justify">Длина значения элемента не должна превышать 256 символов.</li>
            <li class="text-justify">Значение элемента должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">Повторения значений в списке не допускаются.</li>
            <li class="text-justify">Необходимо указать минимум одно значение.</li>
        </ul>
    )
}

export function RCidrSet() {
    return (
        <ul>
            <li class="text-justify">Значение элемента должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">Повторения значений в списке не допускаются.</li>
            <li class="text-justify">Необходимо указать минимум одно значение.</li>
            <li class="text-justify">В пределах одной Security Group и направления трафика (I/E), необходимо обеспечить, непересекаемость диапазонов адресов подсетей.</li>
            <li class="text-justify">Подсеть должна соответствовать формату записи, определенному в RFC 4632.</li>
        </ul>
    )
}

export function RFqdnSet() {
    return (
        <ul>
            <li class="text-justify">Длина значения элемента не должна превышать 256 символов.</li>
            <li class="text-justify">Значение элемента должно начинаться и заканчиваться символами без пробелов.</li>
            <li class="text-justify">Повторения значений в списке не допускаются.</li>
            <li class="text-justify">Необходимо указать минимум одно значение.</li>
        </ul>
    )
}

export function RPorts() {
    return (
        <ul>
            <li class="text-justify">Значения портов должно находиться в интервале от 1 до 65535.</li>
            <li class="text-justify">Если значение не будет указано то будет использоваться весь диапазон портов.</li>
            <li class="text-justify">Значения портов прописываются по одному или интервально используя '-'.</li>
            <li class="text-justify">Не допускаются пересечения портов в правилах в рамках одной пары Security Group.</li>
        </ul>
    )
}

export function RIcmpType() {
    return (
        <ul>
            <li class="text-justify">Значение должно быть числом в диапазоне от 0 до 255.</li>
            <li class="text-justify">Повторения значений в списке не допускаются.</li>
        </ul>
    )
}

export function RItems() {
    return (
        <ul>
            <li class="text-justify">Каждое правило должно обладать уникальным ключом для предотвращения конфликтов.</li>
        </ul>
    )
}

export function RProtocols() {
    return (
        <ul>
            <li class="text-justify">Не допускаются дубликация протоколов в списке.</li>
            <li class="text-justify">Значения должны соответствовать поддерживаемым протоколам.</li>
        </ul>
    )    
}

export function RItemsRuleName({type}) {
    let template = ''
    switch(type) {
        case 's2s':
            template = `"$\{transport\}:sg-local($\{sg_local\})sg($\{sg\})"`;
            break;
        case 's2s-ie':
            template = `"$\{transport\}:sg-local($\{sg_local\})sg($\{sg\})$\{traffic\}"`;
            break;
        case 's2c-ie':
            template = `"$\{transport\}:cidr($\{cidr\})sg($\{sg_name\})$\{traffic\}"`;
            break;
        case 's2f-e':
            template = `"$\{transport\}:sg($\{sg_from\})fqdn($\{fqdn\})"`;
            break;
    }
    return (
        <ul>
            <li class="text-justify">Форма ruleName должна быть организована в соответствии с определенной последовательностью, которую нужно соблюдать <code>{template}</code>.</li>
        </ul>
    )
}