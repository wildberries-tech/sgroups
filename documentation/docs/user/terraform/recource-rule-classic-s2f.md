---
id: recource-rule-classic-s2f
---

## **Описание**

sgroups_fqdn_rules - это ресурс Terraform для определения правил безопасности на основе протокола, исходной группы безопасности (sg_from), и FQDN (Fully Qualified Domain Name).

## **Параметры Ресурса**

| название | обязательность | тип данных | доп. описание |
| --- | --- | --- | --- |
| transport | да | "TCP\|UDP" | Тип протокола для данного правила. Должен быть установлен в "TCP" или "UDP". |
| logs | нет | bool | Определяет, включены ли логи для данного правила. По умолчанию значение "false". Установите в "true", если требуется включить логирование. |
| trace | нет | bool | Определяет, включен ли трассировочный режим для данного правила. По умолчанию значение "false". Установите в "true", если требуется включить трассировку. |
| sg_from | да | string | Имя исходной группы безопасности. |
| fqdn | да | string | Полное доменное имя (FQDN), для которого применяется данное правило. |
| protocols[] | нет | array of strings | Массив протоколов которые будут учитываться при формировании правила. |
| ports[] | нет | array of object | Массив объектов, представляющих порты. Каждый объект содержит поля s (source) и d (destination). |
| ports[].s | нет | string | Значения s (source) портов в рамках одного правила не должны пересекаться |
| ports[].d | нет | string | Значения d (destination) портов в рамках одного правила могут пересекаться |

## **Пример использования**

```hcl
resource "sgroups_fqdn_rules" "rules" {
    items = {
      "TCP:sg(sg-11)fqdn(example.com)" = {
        transport = "TCP"
        logs = true
        trace = true
        sg_from = "sg-11"
        fqdn = "example.com"
        protocols = ["http", "ssh"]
        ports = [
          { s = "80", d = "8080" }
          ]
        }
    }
}
```

## **Ограничения**

* Каждое правило должно иметь уникальный ключ, обеспечивающий отсутствие пересечений.
* Структура формы items должна соответствовать определенной последовательности которой необходимо придерживаться `"${transport}:sg(${sgroup_from})fqdn(${fqdn_to})"`
* sg_from - длина значения не должна превышать 256 символов. Значение должно начинаться и заканчиваться символами без пробелов.
* Значения s (source) портов не должно пересекаться в рамках одного правила состоящего из уникального составного ключа (transport, sg_from, fqdn).
* fqdn - длина значения не должна превышать 256 символов. Значение начинается со строки, которая содержит один или более символов, являющихся буквами нижнего регистра, цифрами, символом '**'** или символам**и '_'** и **'-'** (кроме первого символа, который не может быть '-' или '_'), и должна быть длиной от 1 до 62 символов. затем может следовать любое количество строк, начинающихся с символа '.', за которым идет один символ, являющийся буквой нижнего регистра, цифрой, символом '' или символом '-', и длина строки от 0 до 62 символов. (пример: google.com).
* transport - может быть только одно из двух значений "TCP" или "UDP".
* protocols - Если протокол не указан, то в режиме работы "dpi" и "combine" взаимодействие по указанному правилу будет без учета протокола.